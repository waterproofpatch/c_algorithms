#ifndef _SWAP_H_
#define _SWAP_H_ 

/**
 * Swap two unsigned integers.
 * 
 * @param list: the list in which integers will be swapped.
 * @param pos1: the position to swap
 * @param pos2: the position to swap with
 * */
void swap(unsigned int list[], int pos1, int pos2);

#endif