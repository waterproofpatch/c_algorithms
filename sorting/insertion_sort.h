#ifndef _INSERTION_SORT_H
#define _INSERTION_SORT_H

/**
 * Insertion sort a list of unsigned integers.
 * Computational complexity: Average case: O(n^2). Best case: O(n). Worst case: O(n^2)
 * Spacial complexity: O(n) (in-place sort)
 * */
void sort(unsigned int list[], int length);

#endif